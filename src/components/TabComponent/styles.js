const styles = (theme) => ({
  root: {
    background: "#37474f",
    color: "white",
    margin: "auto",
    padding: "1em",
    textTransform: "uppercase",
  },
  appBar: {
    marginBottom: "0.5em",
  },
});

export default styles;
