const styles = (theme) => ({
  myListEmptyActivity: {
    padding: "1em",
  },
  myListEmptyActivityButton: {
    padding: "1em",
    margin: "1em",
  },
  myListLink: {
    textDecoration: "none",
  },
});
export default styles;
