const styles = (theme) => ({
  drawer: {
    background: "#00A08A",
    color: "#fff",
    padding: "1em",
    textTransform: "uppercase",
  },
  filterButton: {
    marginBottom: "1em",
  },
});

export default styles;
